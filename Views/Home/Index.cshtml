@{
    ViewData["Title"] = "Admin Panel";
}

<h1>Admin Panel</h1>

<div class="row mt-4">
    <div class="col-md-4">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Users</h5>
                <p class="card-text">Manage application users.</p>
                <a href="@Url.Action("Index", "Users")" class="btn btn-primary">Go to Users</a>
            </div>
        </div>
    </div>
</div>